server {
    listen 80;

    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ =404;
    }

    location /files/ {
        autoindex on;
        autoindex_exact_size off;
        autoindex_localtime on;
    }
}